{
   "version": 2,
   "builds": [
      { "src": "src/server.ts", "use": "@vercel/node" },
      { "src": "prisma/seed.ts", "use": "@now/node" }
   ],

   "routes": [
      {
         "src": "/(.*)",
         "dest": "src/server.ts"
      }
   ]
}
